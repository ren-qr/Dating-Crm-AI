# 后台助手 Agent

后台助手已采用 v0.3 Manager 模式，唯一运行实现在 `src/agent-system/runtime/`，提示词在 `src/agent-system/prompts/manager.ts`。本目录只保留旧组织方式的导航，不再创建第二份 Runtime。

能力登记见 `docs/agents.md`，实现说明见 `docs/agent-runtime-v0.3.md`。
